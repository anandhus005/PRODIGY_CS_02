# PRODIGY_CS_02
Create a tool that encrypts and decrypts images by changing pixel values. You can do operations like XOR, swap pixels, or shuffle pixels so the image becomes unreadable, and later decrypt it back
